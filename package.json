{
  "name": "my-phone-friend",
  "version": "1.0.0",
  "private": true,
  "description": "내 폰 안의 친구 (MY PHONE FRIEND) - 나노바나나 화면 배회 펫 & AI 동반자 모바일 앱",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "cap sync android",
    "cap:build": "npm run build && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
